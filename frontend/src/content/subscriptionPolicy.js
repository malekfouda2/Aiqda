import { text } from '../i18n/translations';

export const SUBSCRIPTION_DEVICE_LIMIT_TITLE = text('Account Access Policy', 'سياسة الوصول إلى الحساب');

export const SUBSCRIPTION_DEVICE_LIMIT_DISCLAIMER =
  text(
    'Each account with an active subscription is limited to a maximum of 2 devices / IP addresses for access.',
    'كل حساب يملك اشتراكًا نشطًا يقتصر الوصول إليه على جهازين كحد أقصى أو عنواني IP فقط.'
  );
